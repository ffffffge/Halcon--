<UserControl x:Class="Halcon查询.AIChatView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Halcon查询"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="450">
    <UserControl.Resources>
        <!-- 转换器注册 -->
        <local:UserAlignConverter x:Key="UserAlignConverter"/>
        <local:EmptyStringToCollapsedConverter x:Key="EmptyStringToCollapsedConverter"/>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>

        <!-- 聊天气泡模板 -->
        <DataTemplate x:Key="ChatBubbleTemplate">
            <Grid Margin="8,6">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- 角色标签 -->
                <TextBlock Grid.Row="0" Margin="4,0,4,2" Foreground="#999" FontSize="11"
                           HorizontalAlignment="{Binding IsUser, Converter={StaticResource UserAlignConverter}}">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Text" Value="🤖 AI"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsUser}" Value="True">
                                    <Setter Property="Text" Value="我"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>

                <!-- 气泡 -->
                <Border Grid.Row="1" CornerRadius="10" Padding="12,10"
                        HorizontalAlignment="{Binding IsUser, Converter={StaticResource UserAlignConverter}}" MaxWidth="300">
                    <Border.Style>
                        <Style TargetType="Border">
                            <Setter Property="Background" Value="White"/>
                            <Setter Property="BorderBrush" Value="#E8E8E8"/>
                            <Setter Property="BorderThickness" Value="1"/>
                            <Setter Property="Effect">
                                <Setter.Value>
                                    <DropShadowEffect ShadowDepth="1" BlurRadius="4" Opacity="0.08"/>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsUser}" Value="True">
                                    <Setter Property="Background" Value="#95EC69"/>
                                    <Setter Property="BorderThickness" Value="0"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>
                    <TextBlock Text="{Binding Content}" TextWrapping="Wrap" FontSize="13.5" 
                               Foreground="#333" LineHeight="20"/>
                </Border>

                <!-- 状态指示 -->
                <TextBlock Grid.Row="2" Text="{Binding StatusText}" Foreground="#2196F3" FontSize="11" 
                           Margin="4,3,4,0" FontStyle="Italic"
                           Visibility="{Binding StatusText, Converter={StaticResource EmptyStringToCollapsedConverter}}"/>
            </Grid>
        </DataTemplate>

        <!-- 侧栏会话项模板 -->
        <DataTemplate x:Key="ConversationItemTemplate">
            <Grid Margin="0,1" Cursor="Hand">
                <Border CornerRadius="6" Padding="10,8" Background="Transparent" x:Name="ItemBorder">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="{Binding Title}" Foreground="#E0E0E0" FontSize="13" 
                                       TextTrimming="CharacterEllipsis" FontWeight="Medium"/>
                            <TextBlock Text="{Binding CreatedAt, StringFormat='{}{0:MM/dd HH:mm}'}" 
                                       Foreground="#888" FontSize="10" Margin="0,3,0,0"/>
                        </StackPanel>
                        <!-- 删除按钮 -->
                        <Button Grid.Column="1" Content="✕" FontSize="11" VerticalAlignment="Center"
                                Background="Transparent" Foreground="#888" BorderThickness="0" Cursor="Hand"
                                Command="{Binding DataContext.DeleteConversationCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                CommandParameter="{Binding}" Padding="4,2" Visibility="Hidden" x:Name="DeleteBtn"/>
                    </Grid>
                </Border>
            </Grid>
            <DataTemplate.Triggers>
                <Trigger SourceName="ItemBorder" Property="IsMouseOver" Value="True">
                    <Setter TargetName="ItemBorder" Property="Background" Value="#3A3A4A"/>
                    <Setter TargetName="DeleteBtn" Property="Visibility" Value="Visible"/>
                </Trigger>
            </DataTemplate.Triggers>
        </DataTemplate>
    </UserControl.Resources>

    <!-- 主布局：侧栏 + 聊天区 -->
    <Grid>
        <Grid.ColumnDefinitions>
            <!-- 侧栏 -->
            <ColumnDefinition Width="{Binding SidebarWidth}"/>
            <!-- 聊天区 -->
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- ========== 左侧侧栏 ========== -->
        <Border Grid.Column="0" Background="#2B2B3D" Visibility="{Binding IsSidebarVisible, Converter={StaticResource BoolToVis}}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- 新建对话按钮 -->
                <Button Grid.Row="0" Command="{Binding NewConversationCommand}" Margin="10,12,10,8"
                        Height="36" Background="#3E3E52" Foreground="White" BorderThickness="1" 
                        BorderBrush="#555" Cursor="Hand" FontSize="13">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="＋" FontSize="16" Margin="0,0,6,0" VerticalAlignment="Center"/>
                        <TextBlock Text="新建对话" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>

                <!-- 会话历史列表 -->
                <ListBox Grid.Row="1" ItemsSource="{Binding Conversations}" 
                         SelectedItem="{Binding CurrentConversation, Mode=TwoWay}"
                         ItemTemplate="{StaticResource ConversationItemTemplate}"
                         Background="Transparent" BorderThickness="0" Margin="6,0"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                            <Setter Property="Padding" Value="0"/>
                            <Setter Property="Margin" Value="0"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListBoxItem">
                                        <Border x:Name="Bd" CornerRadius="6" Margin="2,1" Padding="0">
                                            <ContentPresenter/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsSelected" Value="True">
                                                <Setter TargetName="Bd" Property="Background" Value="#4A4A5E"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ListBox.ItemContainerStyle>
                </ListBox>

                <!-- 底部设置按钮 -->
                <Button Grid.Row="2" Command="{Binding OpenSettingsCommand}" Margin="10,8,10,12"
                        Height="32" Background="Transparent" Foreground="#AAA" BorderThickness="0" 
                        Cursor="Hand" FontSize="13" HorizontalContentAlignment="Left">
                    <StackPanel Orientation="Horizontal" Margin="4,0">
                        <TextBlock Text="⚙" FontSize="15" Margin="0,0,8,0" VerticalAlignment="Center"/>
                        <TextBlock Text="设置" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
            </Grid>
        </Border>

        <!-- ========== 右侧聊天区 ========== -->
        <Grid Grid.Column="1" Background="#F5F5F5">
            <Grid.RowDefinitions>
                <RowDefinition Height="46"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- 顶部工具栏 -->
            <Border Grid.Row="0" Background="White" BorderBrush="#E8E8E8" BorderThickness="0,0,0,1">
                <Grid Margin="12,0">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <!-- 侧栏折叠按钮 -->
                        <Button Command="{Binding ToggleSidebarCommand}" Background="Transparent" 
                                BorderThickness="0" Cursor="Hand" Padding="6,4" Margin="0,0,10,0"
                                FontSize="16" Foreground="#666" Content="☰"/>
                        <TextBlock Text="{Binding CurrentTitle}" FontWeight="Bold" FontSize="15" 
                                   Foreground="#444" VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- 对话消息列表 -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" x:Name="ChatScrollViewer">
                <ItemsControl ItemsSource="{Binding CurrentMessages}" 
                              ItemTemplate="{StaticResource ChatBubbleTemplate}" Margin="4,8">
                    <ItemsControl.ItemContainerStyle>
                        <Style TargetType="ContentPresenter">
                            <Setter Property="Focusable" Value="False"/>
                        </Style>
                    </ItemsControl.ItemContainerStyle>
                </ItemsControl>
            </ScrollViewer>

            <!-- 底部输入区 -->
            <Border Grid.Row="2" Background="White" BorderBrush="#E8E8E8" BorderThickness="0,1,0,0" Padding="12,10">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBox Grid.Column="0" Text="{Binding InputText, UpdateSourceTrigger=PropertyChanged}"
                             MinHeight="38" MaxHeight="90" TextWrapping="Wrap" AcceptsReturn="True"
                             VerticalScrollBarVisibility="Auto" Padding="10,8" FontSize="14"
                             VerticalContentAlignment="Center" BorderBrush="#DDD" BorderThickness="1">
                        <TextBox.InputBindings>
                            <KeyBinding Key="Enter" Command="{Binding SendMessageCommand}"/>
                        </TextBox.InputBindings>
                    </TextBox>

                    <Button Grid.Column="1" Command="{Binding SendMessageCommand}" Margin="10,0,0,0"
                            Width="70" Height="38" Content="发送" FontWeight="Bold" FontSize="14"
                            Foreground="White" Background="#0078D7" BorderThickness="0" Cursor="Hand">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Style.Triggers>
                                    <Trigger Property="IsEnabled" Value="False">
                                        <Setter Property="Opacity" Value="0.4"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>
